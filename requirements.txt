# ─────────────────────────────────────────────────────────────────────────────
# HOST requirements (Python 3.10+, Ubuntu 20.04 / 22.04)
# Install with:  pip install -r requirements.txt
#
# These are for running on the workstation:
#   - YOLO detection service     (yolo_service.py)
#   - Face recognition service   (face_recognition_service.py)
#   - Offline evaluation scripts (eval/*.py)
#   - Visualisation tools        (visualizer.py, web_visualizer.py)
#
# The ROS agent code runs INSIDE Docker — see docker/requirements_ros.txt
# ─────────────────────────────────────────────────────────────────────────────

# Object detection (YOLO service)
ultralytics>=8.0.0

# Computer vision
opencv-python>=4.5.0
numpy>=1.22.0
scipy>=1.8.0
Pillow>=9.0.0

# HTTP + API
requests>=2.27.0
flask>=2.0.0

# Face recognition service
face-recognition>=1.3.0

# Evaluation reports
matplotlib>=3.5.0

# Config / environment
python-dotenv>=0.20.0
pyyaml>=6.0

# Voice input (optional — needed for microphone command input)
SpeechRecognition>=3.9.0
# pyaudio>=0.2.11   # uncomment if you want mic input; requires: sudo apt-get install portaudio19-dev
